{"sta":1,"data":{"subTitle":"flex属性","content":"<p><code>flex</code>属性是<code>flex-grow</code>,<span>&nbsp;</span><code>flex-shrink</code><span>&nbsp;</span>和<span>&nbsp;</span><code>flex-basis</code>的简写，默认值为<code>0 1 auto</code>。后两个属性可选。</p><pre><code><span>.item </span><span>{</span>\n  <span>flex</span><span>:</span> none | [ <span>&lt;</span><span>'flex-grow'</span>&gt; <span>&lt;</span><span>'flex-shrink'</span>&gt;? || <span>&lt;</span><span>'flex-basis'</span>&gt; ]\n<span>}</span>\n</code></pre><p>该属性有两个快捷值：<code>auto</code><span>&nbsp;</span>(<code>1 1 auto</code>) 和 none (<code>0 0 auto</code>)。</p><p>建议优先使用这个属性，而不是单独写三个分离的属性，因为浏览器会推算相关值。</p>"}}