{"sta":1,"data":{"subTitle":"flex-basis属性","content":"<p><code>flex-basis</code>属性定义了在分配多余空间之前，项目占据的主轴空间（main size）。浏览器根据这个属性，计算主轴是否有多余空间。它的默认值为<code>auto</code>，即项目的本来大小。</p><pre><code><span>.item </span><span>{</span>\n  <span>flex-basis</span><span>:</span> <span>&lt;</span>length&gt; | auto<span>;</span> <span spellcheck=\"true\">/* default auto */</span>\n<span>}</span>\n</code></pre><p>它可以设为跟<code>width</code>或<code>height</code>属性一样的值（比如350px），则项目将占据固定空间。</p>"}}