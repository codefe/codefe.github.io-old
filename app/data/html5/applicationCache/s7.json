{"sta":1,"data":{"subTitle":"删除一个缓存对象","content":"<p>想要删除一个缓存对象，你只需要缓存的键名即可：&nbsp;&nbsp;<br></p><pre><code>caches.delete('test-cache').then(function() { \n  console.log('Cache successfully deleted!'); \n});</code></pre><p>大量删除旧缓存数据的方法：</p><pre><code>// 假设`CACHE_NAME`是新的缓存的名称\n// 现在清除旧的缓存\nvar CACHE_NAME = 'version-8';\n\n// ...\n\ncaches.keys().then(function(cacheNames) {\n  return Promise.all(\n    cacheNames.map(function(cacheName) {\n      if(cacheName != CACHE_NAME) {\n        return caches.delete(cacheName);\n      }\n    })\n  );\n});</code></pre><p>想成为一个service worker专家？上面的这些代码值得放到你的储备库里。火狐浏览器和谷歌浏览器都支持service worker，相信很快就会有更多的网站、app使用这种缓存技术来提高运行速度。</p>"}}