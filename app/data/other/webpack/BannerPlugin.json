{"sta":1,"data":{"subTitle":"BannerPlugin","content":"<h3 id=\"bannerplugin\">BannerPlugin</h3><pre><code><span>new</span> <span>webpack<span>.</span>BannerPlugin</span><span>(</span>banner<span>)</span>\n<span spellcheck=\"true\">// or</span>\n<span>new</span> <span>webpack<span>.</span>BannerPlugin</span><span>(</span>options<span>)</span>\n</code></pre><p>为每个 chunk 文件头部添加 banner。</p><h3 id=\"选项\">选项</h3><p><!--StartFragment--><!--EndFragment--></p><pre><code><span>{</span>\n  banner<span>:</span> string<span>,</span> <span spellcheck=\"true\">// 其值为字符串，将作为注释存在</span>\n  raw<span>:</span> boolean<span>,</span> <span spellcheck=\"true\">// 如果值为 true，将直出，不会被作为注释</span>\n  entryOnly<span>:</span> boolean<span>,</span> <span spellcheck=\"true\">// 如果值为 true，将只在入口 chunks 文件中添加</span>\n  test<span>:</span> string <span>|</span> RegExp <span>|</span> Array<span>,</span>\n  include<span>:</span> string <span>|</span> RegExp <span>|</span> Array<span>,</span>\n  exclude<span>:</span> string <span>|</span> RegExp <span>|</span> Array<span>,</span>\n<span>}</span></code></pre>"}}